{
  "name": "roam-task-status-tags",
  "version": "0.4.0",
  "private": true,
  "description": "Roam task workflow tags with a fail-closed Better Tasks compatibility boundary",
  "type": "module",
  "main": "extension.js",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify:generated": "node scripts/verify-generated.mjs",
    "check": "npm run build && npm run scan:secrets && node --check extension.js && npm test && npm run verify:generated"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  },
  "license": "UNLICENSED"
}
